#include <iostream>
#include <string>
#include <vector>

bool CheckForValidChars( std::string word );
std::string WordToPronunciation( std::vector<std::string> vect );
std::vector<std::string> HandleSpaces( std::string word );